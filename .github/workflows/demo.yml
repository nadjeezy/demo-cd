name: demo 

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World"
      - uses: actions/checkout@main
      - uses: azure/login@v1
        with:
          creds: ${{ secret.AZURE_CREDENTIALS }}